# Environment variables for local development (outside Docker)
# When running inside Docker, these are set by docker-compose.yml

# Kafka Configuration
KAFKA_BOOTSTRAP_SERVERS=localhost:9092

# MinIO Configuration
MINIO_ENDPOINT=http://localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin

# Iceberg Configuration
ICEBERG_WAREHOUSE=s3a://warehouse
CHECKPOINT_LOCATION=/tmp/checkpoints/iceberg_bronze
